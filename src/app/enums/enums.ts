export enum levels {
  easy = 4,
  medium = 6,
  hard = 8,
}

export enum levelNames {
  easy = 'easy',
  medium = 'medium',
  hard = 'hard',
}

export enum languages {
  es,
  en,
}


export enum tileStates {
  show,
  hidden,
}